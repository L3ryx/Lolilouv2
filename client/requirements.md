## Packages
react-dropzone | Beautiful drag and drop file upload interactions
framer-motion | Page transitions and list stagger animations for a premium feel

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  body: ["var(--font-sans)"],
}
The endpoint `/api/analyze` expects a multipart/form-data request with the field name `images`.
